<div class="container">
  <h1>Result</h1>
    <% if !@match %>
      <p>Sorry but <strong><%= @word %></strong> cannot be built out of <%= @letters %></p>
    <% elsif @word_api%>
      <p><strong>Congratulations! </strong><%= @word %> is a valid English word!</p>
      <%= hidden_field_tag :score, @score = true %>
    <% else %>
      <p>Sorry but <strong><%= @word %></strong> does not seem to be a valid English word...</p>
    <% end  %>
    <p>Total score: <%= session[:current_points] %></p>
    <%= link_to 'Play again', new_path %>
</div>
